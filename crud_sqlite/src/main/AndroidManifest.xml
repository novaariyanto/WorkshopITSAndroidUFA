<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.sch.smktiufa.workshopitsandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<activity android:name=".activity.ActivityNews">
    <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
    </intent-filter>
</activity>
        <provider
            android:authorities="id.sch.smktiufa.workshopitsandroid.content.newscontentprovider"
            android:name="id.sch.smktiufa.workshopitsandroid.content.NewsContentProvider"
            android:syncable="true"
            />
    </application>

</manifest>
